<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->

<head>
  <%= render partial: "shared/layout/head" %>
  <%= stylesheet_link_tag :application, :debug => Rails.env.development? %>
</head>
<body lang="en" class="<%= yield (:class_body) %>">

  <%= render partial: "shared/layout/header" %>

  <section id="content" class="l-section-x"><div>

    <%- if @page_title.any? %>
    <header class="page-header">
      <h1><%= @page_title.first %></h1>
    </header>
    <% end %>

    <%- if page_namespace.in?(%w(read store about)) %>
    <nav class="sidebar">
      <%= render partial: "shared/nav/#{page_namespace}" %>
    </nav>
    <%- end %>

    <main role="main" class="<%= yield (:class_main) %>">
      <%= yield %>
    </main>

  </div></section>

  <%= render partial: "shared/layout/footer" %>

  <%= javascript_include_tag :application, :debug => Rails.env.development? %>
  <%= yield :footer %>
</body>
</html>