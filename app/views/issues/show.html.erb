<% title "Issue #{@issue.number}"  %>

<% cache @issue do %>

  <div class="l-line-t l-p-y tagline issue-<%= @issue.number %> c-issue">
    <a href="<%= legacy_store_issue_url(@issue.number) %>" rel="external" class="button is-large">Buy Issue <%= @issue.number %> in Print</a>
    or <%= link_to 'back our Kickstarter project', kickstarter_url, rel: 'external' %>
    and support The Manual.
  </div>

  <ul class="l-issue issue-<%= @issue.number %>">
    <% @issue.articles.ordered.each do |article| %>
    <li>
      <figure class="l-issue-illo">
        <%= illustration_tag(article, size: 'm') %>
      </figure>
      <div class="l-issue-text">

        <h2 class="piece-unit-author"><%= article.author.name %></h2>

        <div class="piece-unit-entry">
          <h3 class="piece-unit-title">
            <%= link_to article.title, piece_path(@issue.number, article.author_slug, :article)  %>
          </h3>
          <p class="piece-unit-synopsis is-short">
            <%= article.synopsis %>
          </p>
          <p class="piece-unit-metadata">
            <span class="piece-unit-topics"><a href="#">Thing</a>, <a href="#">Other</a>, <a href="#">Another</a></span>
          </p>
        </div>

        <div class="piece-unit-entry">
          <h3 class="piece-unit-title">
            <%= link_to article.lesson.title, piece_path(@issue.number, article.author_slug, :lesson) %>
          </h3>
          <p class="piece-unit-synopsis is-short">
            <%= article.lesson.synopsis %>
          </p>
          <p class="piece-unit-metadata">
            <span class="piece-unit-topics"><a href="#">Thing</a>, <a href="#">Other</a>, <a href="#">Another</a></span>
          </p>
        </div>
      </div>
    </li>
    <% end %>
  </ul>

<% end %>