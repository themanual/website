<% if @user.first_name.present? %>Hi <%= @user.first_name %>.<% else %>Hi there.<% end %>

Follow this link to log in:
<%= login_token_url(@token.token) %>

This login expires in 24h. Didn't try to log in? Ignore this email.