Hi <%= @data[:user].first_name %>

As part of your current subscription to The Manual (<%= @data[:subscription].issues_names %>), you now have access to the web edition of <%= @data[:issue_number] %>. 

When logged in, you can read Issue <%= @data[:issue_number] %> online at <%= issue_url(@data[:issue_number]) %>.

When prompted to log in, there is no password, just enter your email address (<%= @data[:user].email %>) and we’ll email you a link to log in. You can access your account details at <%= account_url %>.

Thanks,

— Your friends at The Manual