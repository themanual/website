
<meta charset="utf-8" />
<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<%# Prepare title %>
<%- title "The Manual", :push -%>
<%- metadata["og:title"] ||= title.first -%>

<title><%= title.join(' · ') %></title>

<%- metadata.each do |prop, value| -%>
<%= tag "meta", (prop =~ /^og:/ ? "property" : "name") => prop, "content" => value %>
<%- end -%>

<%= csrf_meta_tags %>

<%# CSS %>
<%= stylesheet_link_tag :application, :debug => Rails.env.development? %>

<%# JS %>
<script>(function(H){H.className=H.className.replace(/\bno-js\b/,'js')})(document.documentElement)</script>
<!--[if lt IE 9]><script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js"></script><![endif]-->
<%= javascript_include_tag :head, :debug => Rails.env.development? %>
<%= render partial: "shared/segmentio" if Rails.env.production? %>

<script type="text/javascript" src="//use.typekit.net/<%= Rails.env.production? ? "wnf5tgz" : "zve6rgt" -%>.js"></script>
<script type="text/javascript">try{Typekit.load();}catch(e){}</script>