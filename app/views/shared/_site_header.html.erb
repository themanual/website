<header class="site-header"><div class="l-grid">
  <h1><a href="<%= root_url %>">The Manual</a></h1>
  <nav>
    <ul class="inline-list">
      <li><%= link_to "Read", {controller: "home", action: "staffpicks"}, class: (defined? current and current == "read" ? "current" : nil) %></li>
      <li><%= link_to "Shop", store_path,   class: (defined? current and current == "shop" ? "current" : nil) %></li>
      <li><a href="#">About</a></li>
      <li class="l-fl-r"><%= user_signed_in? ? link_to("Log out", destroy_user_session_path) : link_to("Log in", new_user_session_path) %></li>
    </ul>
  </nav>
</div></header>